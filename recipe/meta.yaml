{% set name = "laz-perf" %}
{% set version = "1.2.0" %}
{% set sha256 = "eb36d90d470db1f16eb9e3c898d6be98c7f16507dfecd894426e6e2a5fb25bee" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/hobu/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [win]
  features:
    - vc14  # [win]
  
requirements:
  build:
    - cmake
    - toolchain
    - vc 14  # [win]
  run:
    - vc 14  # [win]

test:
  commands:
    - test -d ${PREFIX}/include/laz-perf  # [not win]

about:
  home: https://github.com/hobu/laz-perf
  license: LGPL
  summary: 'Alternative LAZ implementation for C++ and JavaScript'
  description: |
    Alternative LAZ implementation for C++ and JavaScript..
  doc_url: https://github.com/hobu/laz-perf
  dev_url: https://github.com/hobu/laz-perf

extra:
  recipe-maintainers:
    - chambbj
